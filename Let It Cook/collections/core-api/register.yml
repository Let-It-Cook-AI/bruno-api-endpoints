info:
  name: register
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8080/core-api/account/register
  body:
    type: json
    data: |-
      {
        "email": "basil.kitana19@gmail.com",
        "password": "12345678",
        "username": "basil"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
