info:
  name: Invite Sous Chef
  type: http
  seq: 4

http:
  method: POST
  url: http://localhost:8080/core-api/session/{id}/invite
  body:
    type: json
    data: |-
      {
        "guestUserId": "guest_456"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
