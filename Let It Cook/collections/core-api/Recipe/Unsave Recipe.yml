info:
  name: Unsave Recipe
  type: http
  seq: 15

http:
  method: POST
  url: http://localhost:8080/core-api/recipe/{id}/unsave
  body:
    type: none
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
