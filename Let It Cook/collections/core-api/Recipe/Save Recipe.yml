info:
  name: Save Recipe
  type: http
  seq: 14

http:
  method: POST
  url: http://localhost:8080/core-api/recipe/{id}/save
  body:
    type: none
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
