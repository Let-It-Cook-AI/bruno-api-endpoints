info:
  name: Generate Steps
  type: http
  seq: 17

http:
  method: POST
  url: http://localhost:8080/core-api/recipe/{id}/generate-steps
  body:
    type: none
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
